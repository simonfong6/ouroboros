# ncurses on mac.
# Uses the homebrew installed ncurses.
# https://github.com/ourarash/ncurses_bazel

cc_library(
   name = "main",
   srcs = ["lib/libncurses.a"],
   visibility = ["//visibility:public"],
)
